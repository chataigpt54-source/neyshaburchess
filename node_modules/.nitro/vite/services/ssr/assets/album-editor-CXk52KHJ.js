import { g as adminSaveAlbum } from "./admin-C62riz4W.js";
import { t as Button } from "./button-CeZ5edYX.js";
import { i as Textarea, n as Input, r as Label } from "./input-Crt41rXZ.js";
import { n as MultiImageUploader } from "./uploader-MNxtFenC.js";
import { useState } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
//#region src/components/admin/album-editor.tsx
function AlbumEditor({ initial }) {
	const navigate = useNavigate();
	const [title, setTitle] = useState(initial?.title ?? "");
	const [description, setDescription] = useState(initial?.description ?? "");
	const [albumDate, setDate] = useState(initial?.albumDate ?? "");
	const [coverMediaId, setCover] = useState(initial?.coverMediaId ?? null);
	const [imageIds, setIds] = useState(initial?.images.map((i) => i.mediaId) ?? []);
	const [busy, setBusy] = useState(false);
	async function save() {
		if (!title.trim()) {
			toast.error("عنوان گالری را وارد کنید.");
			return;
		}
		setBusy(true);
		try {
			await adminSaveAlbum({ data: {
				id: initial?.id,
				title,
				description,
				albumDate: albumDate || null,
				coverMediaId,
				imageIds
			} });
			toast.success("گالری ذخیره شد.");
			await navigate({ to: "/admin/gallery" });
		} catch (e) {
			toast.error(e instanceof Error ? e.message : "ذخیره ناموفق بود.");
		} finally {
			setBusy(false);
		}
	}
	return /* @__PURE__ */ jsxs("div", {
		className: "max-w-3xl space-y-4",
		children: [
			/* @__PURE__ */ jsx("h2", {
				className: "text-xl font-semibold",
				children: initial ? "ویرایش گالری" : "افزودن گالری"
			}),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "عنوان گالری" }), /* @__PURE__ */ jsx(Input, {
				value: title,
				onChange: (e) => setTitle(e.target.value)
			})] }),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "توضیحات" }), /* @__PURE__ */ jsx(Textarea, {
				value: description,
				onChange: (e) => setDescription(e.target.value)
			})] }),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "تاریخ" }), /* @__PURE__ */ jsx(Input, {
				type: "date",
				value: albumDate ?? "",
				onChange: (e) => setDate(e.target.value)
			})] }),
			/* @__PURE__ */ jsx(MultiImageUploader, {
				ids: imageIds,
				onChange: setIds,
				coverId: coverMediaId,
				onCover: setCover
			}),
			/* @__PURE__ */ jsx(Button, {
				onClick: () => void save(),
				disabled: busy,
				children: busy ? "در حال ذخیره…" : "ذخیره"
			})
		]
	});
}
//#endregion
export { AlbumEditor as t };
