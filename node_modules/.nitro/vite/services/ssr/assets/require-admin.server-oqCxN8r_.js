import { r as getSql } from "./db-B81SbFjo.js";
import { t as UnauthorizedError } from "./verify.server-BczJSy53.js";
//#region src/lib/require-admin.server.ts
var ForbiddenError = class extends Error {
	status = 403;
	constructor() {
		super("Forbidden");
		this.name = "ForbiddenError";
	}
};
async function isAdminUser(userId) {
	return (await (await getSql())`
    select role from profiles where user_id = ${userId} limit 1
  `)[0]?.role === "admin";
}
async function requireAdmin(userId) {
	if (!userId) throw new UnauthorizedError();
	if (!await isAdminUser(userId)) throw new ForbiddenError();
}
//#endregion
export { requireAdmin as n, isAdminUser as t };
