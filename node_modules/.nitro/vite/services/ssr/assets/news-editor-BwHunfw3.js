import { _ as adminSaveNews } from "./admin-C62riz4W.js";
import { t as Button } from "./button-CeZ5edYX.js";
import { i as Textarea, n as Input, r as Label } from "./input-Crt41rXZ.js";
import { n as MultiImageUploader, t as ImageUploader } from "./uploader-MNxtFenC.js";
import { useState } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
//#region src/components/admin/news-editor.tsx
function NewsEditor({ initial }) {
	const navigate = useNavigate();
	const [title, setTitle] = useState(initial?.title ?? "");
	const [summary, setSummary] = useState(initial?.summary ?? "");
	const [content, setContent] = useState(initial?.content ?? "");
	const [coverMediaId, setCover] = useState(initial?.coverMediaId ?? null);
	const [imageIds, setImageIds] = useState(initial?.imageIds ?? []);
	const [status, setStatus] = useState(initial?.status ?? "draft");
	const [busy, setBusy] = useState(false);
	async function save() {
		if (!title.trim()) {
			toast.error("عنوان را وارد کنید.");
			return;
		}
		setBusy(true);
		try {
			await adminSaveNews({ data: {
				id: initial?.id,
				title,
				summary,
				content,
				coverMediaId,
				status,
				imageIds
			} });
			toast.success("خبر ذخیره شد.");
			await navigate({ to: "/admin/news" });
		} catch (e) {
			toast.error(e instanceof Error ? e.message : "ذخیره ناموفق بود.");
		} finally {
			setBusy(false);
		}
	}
	return /* @__PURE__ */ jsxs("div", {
		className: "max-w-3xl space-y-4",
		children: [
			/* @__PURE__ */ jsx("h2", {
				className: "text-xl font-semibold",
				children: initial ? "ویرایش خبر" : "افزودن خبر"
			}),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "عنوان" }), /* @__PURE__ */ jsx(Input, {
				value: title,
				onChange: (e) => setTitle(e.target.value)
			})] }),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "خلاصه" }), /* @__PURE__ */ jsx(Textarea, {
				value: summary,
				onChange: (e) => setSummary(e.target.value)
			})] }),
			/* @__PURE__ */ jsxs("div", { children: [
				/* @__PURE__ */ jsx(Label, { children: "متن کامل" }),
				/* @__PURE__ */ jsx(Textarea, {
					className: "min-h-48",
					value: content,
					onChange: (e) => setContent(e.target.value)
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-1 text-xs text-muted",
					children: "از مارک‌داون ساده مانند **پررنگ** و لینک [متن](https://...) می‌توانید استفاده کنید."
				})
			] }),
			/* @__PURE__ */ jsx(ImageUploader, {
				value: coverMediaId,
				onChange: setCover,
				label: "تصویر اصلی"
			}),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
				className: "mb-2 text-sm font-medium",
				children: "تصاویر بیشتر"
			}), /* @__PURE__ */ jsx(MultiImageUploader, {
				ids: imageIds,
				onChange: setImageIds
			})] }),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "وضعیت" }), /* @__PURE__ */ jsxs("select", {
				className: "h-11 w-full rounded-[var(--radius-sm)] border border-line bg-white px-3 text-sm",
				value: status,
				onChange: (e) => setStatus(e.target.value),
				children: [/* @__PURE__ */ jsx("option", {
					value: "draft",
					children: "پیش‌نویس"
				}), /* @__PURE__ */ jsx("option", {
					value: "published",
					children: "انتشار"
				})]
			})] }),
			/* @__PURE__ */ jsx(Button, {
				onClick: () => void save(),
				disabled: busy,
				children: busy ? "در حال ذخیره…" : "ذخیره"
			})
		]
	});
}
//#endregion
export { NewsEditor as t };
