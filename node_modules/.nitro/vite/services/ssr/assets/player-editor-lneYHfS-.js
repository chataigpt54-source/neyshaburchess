import { y as adminSavePlayer } from "./admin-C62riz4W.js";
import { t as Button } from "./button-CeZ5edYX.js";
import { i as Textarea, n as Input, r as Label } from "./input-Crt41rXZ.js";
import { t as ImageUploader } from "./uploader-MNxtFenC.js";
import { useState } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
//#region src/components/admin/player-editor.tsx
function PlayerEditor({ initial }) {
	const navigate = useNavigate();
	const [firstName, setFirst] = useState(initial?.firstName ?? "");
	const [lastName, setLast] = useState(initial?.lastName ?? "");
	const [fideId, setFide] = useState(initial?.fideId ?? "");
	const [bio, setBio] = useState(initial?.bio ?? "");
	const [photoMediaId, setPhoto] = useState(initial?.photoMediaId ?? null);
	const [achievements, setAchievements] = useState(initial?.achievements.map((a) => ({
		title: a.title,
		year: a.year ?? "",
		description: a.description
	})) ?? []);
	const [busy, setBusy] = useState(false);
	async function save() {
		if (!firstName.trim() || !lastName.trim()) {
			toast.error("نام و نام خانوادگی را وارد کنید.");
			return;
		}
		setBusy(true);
		try {
			await adminSavePlayer({ data: {
				id: initial?.id,
				firstName,
				lastName,
				fideId,
				bio,
				photoMediaId,
				achievements
			} });
			toast.success("بازیکن ذخیره شد.");
			await navigate({ to: "/admin/players" });
		} catch (e) {
			toast.error(e instanceof Error ? e.message : "ذخیره ناموفق بود.");
		} finally {
			setBusy(false);
		}
	}
	return /* @__PURE__ */ jsxs("div", {
		className: "max-w-3xl space-y-4",
		children: [
			/* @__PURE__ */ jsx("h2", {
				className: "text-xl font-semibold",
				children: initial ? "ویرایش بازیکن" : "افزودن بازیکن"
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-4 sm:grid-cols-2",
				children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "نام" }), /* @__PURE__ */ jsx(Input, {
					value: firstName,
					onChange: (e) => setFirst(e.target.value)
				})] }), /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "نام خانوادگی" }), /* @__PURE__ */ jsx(Input, {
					value: lastName,
					onChange: (e) => setLast(e.target.value)
				})] })]
			}),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "آیدی فیده" }), /* @__PURE__ */ jsx(Input, {
				value: fideId,
				onChange: (e) => setFide(e.target.value)
			})] }),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "بیوگرافی" }), /* @__PURE__ */ jsx(Textarea, {
				value: bio,
				onChange: (e) => setBio(e.target.value)
			})] }),
			/* @__PURE__ */ jsx(ImageUploader, {
				value: photoMediaId,
				onChange: setPhoto,
				label: "عکس"
			}),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs("div", {
				className: "mb-2 flex items-center justify-between",
				children: [/* @__PURE__ */ jsx("p", {
					className: "text-sm font-medium",
					children: "افتخارات"
				}), /* @__PURE__ */ jsx(Button, {
					variant: "outline",
					size: "sm",
					onClick: () => setAchievements((a) => [...a, {
						title: "",
						year: "",
						description: ""
					}]),
					children: "افزودن افتخار"
				})]
			}), /* @__PURE__ */ jsx("div", {
				className: "space-y-3",
				children: achievements.map((a, i) => /* @__PURE__ */ jsxs("div", {
					className: "rounded-[var(--radius-md)] border border-line p-3",
					children: [
						/* @__PURE__ */ jsx(Input, {
							className: "mb-2",
							placeholder: "عنوان افتخار",
							value: a.title,
							onChange: (e) => {
								const next = [...achievements];
								next[i] = {
									...a,
									title: e.target.value
								};
								setAchievements(next);
							}
						}),
						/* @__PURE__ */ jsx(Input, {
							className: "mb-2",
							placeholder: "سال",
							value: a.year,
							onChange: (e) => {
								const next = [...achievements];
								next[i] = {
									...a,
									year: e.target.value
								};
								setAchievements(next);
							}
						}),
						/* @__PURE__ */ jsx(Textarea, {
							placeholder: "توضیح",
							value: a.description,
							onChange: (e) => {
								const next = [...achievements];
								next[i] = {
									...a,
									description: e.target.value
								};
								setAchievements(next);
							}
						}),
						/* @__PURE__ */ jsx(Button, {
							variant: "ghost",
							size: "sm",
							className: "mt-2",
							onClick: () => setAchievements(achievements.filter((_, j) => j !== i)),
							children: "حذف"
						})
					]
				}, i))
			})] }),
			/* @__PURE__ */ jsx(Button, {
				onClick: () => void save(),
				disabled: busy,
				children: busy ? "در حال ذخیره…" : "ذخیره"
			})
		]
	});
}
//#endregion
export { PlayerEditor as t };
